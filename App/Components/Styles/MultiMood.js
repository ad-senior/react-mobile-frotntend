import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    flexRow: {
        flex: 0,
        flexDirection: 'row',
    },
    alignItems: {
        alignItems: 'center'
    },
    image: {
        height:40,
        width: 40,
        borderRadius: 64 / 2
    },
});
